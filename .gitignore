# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# B10 (2026-07-16): duże fixture audio testowe i podpisane dokumenty z podpisami/danymi
# osobowymi — nie należą do historii git, workspace-lokalne. Pliki zostają na dysku,
# tylko nietrackowane.
context/transkrypty-testowe/
context/PELNOMOCNICTWO_PODPISANE.pdf

# backup JSON z scripts/migrate-notion-to-supabase.mjs — pełny eksport danych klientów
# (telefony, e-maile, kwoty), workspace-lokalny, nie do historii git.
backups/

# zrzuty Playwright/debug ze scratch sesji, nie do historii git
.screens/
--full-page

.vercel
